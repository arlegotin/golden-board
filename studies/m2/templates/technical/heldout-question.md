# Held-out observations

For each supplied held-out observation, apply the method you developed from the
clean artifact. Report the resulting artifact state and only those recovered
facts supported by checks in that observation. Do not force a recovery when the
correct conclusion is ambiguity, failure, or a resource limit.
