# Technical evaluator criteria

Score only retained exported evidence. Require the exact candidate binding,
reproducible clean recovery, correct handling of each held-out channel, the
content-query result, and a final account that distinguishes checks from
assumptions. A pass requires no critical hint or answer revelation and no
unresolved rival consistent with the supplied checks.
