# M2 participant learnings and revision status

This ledger accounts for all 31 findings from the original technical answers,
both technical continuations, and the visual learner trial. It records repairs
and their evidence limits; it adds no gate or participant administration.
“Closed in source” means the revised bytes or handout implement the repair,
not that a new carrier or independent learner has passed. Important, potentially
profound, benign material can reasonably require sustained effort, wrong starts
and revisiting examples. No names, dates, consent flow or reconstructed timing
are needed here.

Historical evidence remains under `artifacts/quiz/` (`Q` below): `ANALYSIS.md`,
the original `01-start`–`04-wrap-up` answers, every `05-continue` and `06-fresh`
stage's answers/analysis and saved replay, and `07-learner`'s answer, verification
and assessment audit. The detailed local audit is
`artifacts/work/participant-revision/all-participant-findings.md`. Original
submissions and exposed packets are not rewritten to match this revision.

## Recovery-teaching repair after revised technical trials 08 and 10

The later trials preserve the earlier findings below and sharpen C05. Trial10
independently recovered the clean carrier, both exact streams and all136
sections, implemented the VM/decompression, answered all12 technical predictions,
and reconstructed the queried canonical67-byte position correctly. Those are
substantial successes. Its saved method plus thin adapter did not recover the
held-out observations: the gaps were local-check handling, unknown symbols and
the observation-to-unit bridge. Its subsequent CRC-guided recovery program
recovered useful results but changed the saved method after that checkpoint.
Trial08's helped continuation independently demonstrates that the all-lane/raw
repetition procedure can be acquired after explicit guidance. Neither result
qualifies the earlier saved-method checkpoint. Detailed unaltered answers and
analysis remain in the08/10 quiz folders.

The old primary fact10 VM example merely concatenated integers; executing it
did not exercise the group decision described elsewhere in the carried bytes.
The repair replaces only active teaching recipe110 with a finite executable
decision and links it to actual whole-block constructions. It keeps all lane
and raw-repetition candidates, compares complete checked191-byte values,
deduplicates identical values, rejects conflicts and checks expected physical
identity. A new five-lane erasure construction makes each individual lane fail
while original known/unknown symbols still recover A through repetition.
Unknown stored zeroes never become known observations. The physical allocation
example now names the same section400 as its actual A/B blocks.

All ten carried decision traces are independently reconstructed and executed
by the observed VM in both implementations; every execution is charged. The
finite two-class example is not a complete carrier decoder or a widened error
correction promise. Clean/content/lesson teaching and all previous obligations
remain. Extra effort, trials, backtracking and multiple sessions remain welcome;
the next blind trial tests whether the revised material teaches the complete
procedure before the held-outs, rather than testing speed.

Use the default focused-check/provisional-trial order in
`spec/m2-participant-trials-v1.md`. After focused readiness, a fresh participant
receives the normal four stages without extra hints or forms. Exhaustive damage,
native/Linux production and release wait for stable provisional success. All
carrier identities and full-run measurements below are historical checkpoints
of the preceding revision; they do not validate this edited source. Current
focused results and exact handoff identities belong in the next frozen trial's
ordinary owner notes. M2 remains In progress; no new human pass is claimed.

## What the successful trials establish

- The original four stages established real partial route/recipe discoveries,
  but left no runnable decoder or complete output. Their supplied matrix ABI
  was wrong; rejection under that ABI was reasonable.
- `05-continue` eventually reproduced clean, known-erasure, unknown-error and
  eight diagnostic-probe outcomes after explicit storage, framing, check,
  geometry, state and query help. Earlier terminal-fixed outputs came from a
  reviewer copy; later continuation used that copy. They are assisted results.
- `06-fresh` discovered the true geometry before a geometry hint, then executed
  20 and 24 examples and recovered the clean streams after explicit sector,
  descriptor, VM, whole-unit, mapping and content conventions. The first actual
  protected block in stage 4 was decoded by the reviewer using participant
  code. The participant's final meaning answer was computed without supplied
  answer bytes; its program remains clean-only and narrowly handles that move.
- `07-learner` achieved 12/12 on its separate 45-page visual prototype, including
  seven first-commit practice matches. Exact command replay supports that score
  and the substantive learned representation. It does not measure thinking
  time, prove independent transport discovery or qualify the new 65-page slice.

## Source and validation key

The table uses these links to keep each finding compact. They identify owners
and relevant checks; a link alone does not assert a new test was run.

- **Packet:** [technical templates](templates/technical/),
  [production mechanics tests](../../python/tests/test_m2_participant_mechanics.py).
- **Lesson:** [author](../../tools/m2/learner_content.py),
  [compiler](../../tools/m2/learner_compile.py),
  [semantic tests](../../python/tests/test_m2_learner_content.py).
- **Slice:** [contract](../../spec/slice-v1.md),
  [Python compiler](../../python/golden_board/m2_slice_v1.py),
  [Python tests](../../python/tests/test_m2_slice_v1.py),
  [Rust compiler](../../crates/gb-slice/src/v1.rs),
  [Rust tests](../../crates/gb-slice/tests/slice_v1.rs).
- **UI:** [generic interpreter](../../tools/m2/learner_web/viewer.js),
  [controls](../../tools/m2/learner_web/ui.js),
  [layout](../../tools/m2/learner_web/layout.js),
  [interpreter replay](../../tools/m2/learner_web/test-viewer.js),
  [layout tests](../../tools/m2/learner_web/test-layout.js),
  [browser test](../../tools/m2/learner_web/test-browser.js).
- **Acceptance:** [decoder](../../python/golden_board/m2_decoder.py),
  [decoder tests](../../python/tests/test_m2_decoder.py),
  [bootstrap tests](../../python/tests/test_bootstrap.py),
  [recipe tests](../../python/tests/test_m2_recipe.py),
  [typed content tests](../../python/tests/test_content.py).
- **Carried route:** [new route owner](../../spec/route-v2.md),
  [numeric definitions](../../spec/route-definitions-v2.md),
  [content/control miniature](../../spec/content-teaching-v2.md),
  [VM discriminators](../../spec/recipe-teaching-v2.md),
  [definition checks](../../python/tests/test_m2_route_definitions_v2.py),
  [complete route checks](../../python/tests/test_m2_route_v2.py).
  Historical bootstrap-v1 remains unchanged. C01–C11 below retain the
  acquisition obligations beyond source construction.
- **Recovered use:** [finite evidence owner](../../spec/knowledge-use-v2.md),
  [observation-only relationship checks](../../python/golden_board/m2_route_semantics_v2.py),
  [context and mutation evidence](../../python/golden_board/m2_knowledge_v2.py),
  [focused checks](../../python/tests/test_m2_knowledge_v2.py).
  This is automated evidence of specific uses, not a fresh participant pass.
- **Observed recovery and first use:** [recovery provenance](../../spec/recovery-provenance-v2.md),
  [first-use coverage](../../spec/first-use-v2.md),
  [recovery checks](../../python/tests/test_m2_recovery_provenance_v2.py),
  [first-use checks](../../python/tests/test_m2_first_use_v2.py).
  These bind actual observed prefixes, checked bodies and streams, and the
  finite grounding/use of every retained recipe and table. They do not prove
  human acquisition or replace the complete damage/release checks.
- **Fit:** [capacity adapter](../../python/golden_board/m2_capacity_v1.py),
  [measurement](../../tools/m2/measure_slice_v1.py),
  [scope authority](../../docs/roadmap.md).

## Complete finding ledger

The finding rows retain the original assistance boundaries. The later
development-integration section records implemented carried repairs; a
remaining acquisition obligation means fresh recipient evidence is still
needed, not that the revised teaching bytes are absent.

| ID | Established finding and assistance boundary | Current disposition and closing check |
|---|---|---|
| M01 | Original storage handout/fixture used a four-byte matrix side; production uses u16. This was a supplied-material defect. | **Closed in Packet source; packaging pending.** Real serializers/parsers test corrected matrix, erasures, MSB-first bits, unused low bits, ordered/omitted unit IDs and malformed frames. Regenerated kit must carry those exact files. |
| M02 | The initial opening omitted important/profound/benign/worth sustained effort; later assistance restored that premise. This is motivation, not geometry help. | **Closed in Packet source; packaging pending.** Opening carries the complete premise and permits revised hypotheses without answer hints or participant ceremony. |
| M03 | Exact old content had four reachable generic nodes and 74 opaque payloads, not a required chess teaching sequence. This was established by replay, not a failed learner trial. | **Closed in Lesson/Slice source and development recovery.** All 65 pages are in required content; the all stream adds a neutral one-region library entry exposing all 64 games and ten fixtures, then enters mechanics. Python/JS replay and independent compilers agree. Actual matrix recovery reproduces both streams; qualifying release and fresh human evidence remain pending. |
| M04 | Recipient mechanics named TEXT record 1, exceeded entry budget before COMMIT, retained a reset selection and named a heldout entry without a supported route. | **Carried mechanics and revised development package closed.** New entry 12 carries select 1→reset→select 2→commit with budget 16 and reachable finals. The versioned runner and nine-file package use the recovered revised stream; all65 pages and the8320-command boundary replay pass. Historical v0 runners remain historical. Release packaging remains pending. |
| M05 | Old final self-check illustration left the bypassed pawn; correct response did not isolate king safety. Preserve the original score. | **Closed in Lesson/Slice source.** Rejected EP depiction clears source and bypassed square; rejected castles move the rook. New final uses a novel ordinary pinned-knight move with equal state footers. Authoritative legality and actual displayed cells are checked. |
| M06 | Old record question had only one legal answer; supplied decoded triple also limits any binary-decoding claim. | **Closed for move consequence.** Revised final has two legal outcomes, equal state footers, exactly one matching the record. Lesson tests verify both with the real oracle. Triple remains supplied: no independent Move16 packing claim. |
| M07 | Learner inferred state roles but targets 21/43/45 remained opaque; square addresses arrived too late. Technical export showed the same ambiguity. | **Closed in Lesson/Slice source.** Addresses precede fields; no target 0 versus index+1 and expiry are contrasted; all 16 rights masks/four bits bind to four actual castles. King/rook return restores occupancy but not rights. Tests check displayed canonical states, both sides and nonrestoration. |
| M08 | Queen movement and ordinary king movement were absent before dependent questions; promotions alone do not teach movement. | **Closed at slice scope.** Queen diagonal, blocked/clear file and ordinary king one-step contrasts precede masks/special decisions. Stable paired side codes remain. Tests bind examples/order to oracle truth; no complete-chess-course claim. |
| M09 | Bare history scalar was opaque; supplied counts and byte-identical checkpoints supported only a local threshold inference. | **Closed for stated local meanings.** Visible histories bind repeat/count/claim; nominal versus effective EP is contrasted; legal continuation shows an available claim is not automatic termination. Final shows intermediate positions and no supplied count, using a novel two-occurrence negative case. Broader draw/equivalence acquisition is unclaimed. |
| M10 | Wide diagrams caused mechanical comparison/scrolling effort, although the learner remained oriented and succeeded. Effort itself is acceptable. | **Closed in source at reported scope.** Whole panels wrap at 36 columns without changing selectable cells; generic fit/zoom preserves readable cells, stable regions and scroll. Zoom out disables when every visible matrix is at its readable minimum. Arbitrary rectangles remain supported; narrow screens may still scroll. UI and Slice checks cover these properties. |
| T01 | Both technical histories crossed sector boundaries; fresh geometry was independently correct, but insertion/variable-frame theories were not. Sector correction was supplied. | **Implemented and checked in carried C01; fresh acquisition pending.** Four oriented prefixes are extracted from the actual matrix; finite definitions cover transforms, polarity and boundaries. Bytes after a valid declared prefix are ignored. No deletion search is supplied as meaning. |
| T02 | Descriptor bytes and zero high halves concealed mistaken field widths; the exact ABI was later supplied. | **Implemented and checked in C01/C02; fresh acquisition pending.** The historical expanded format uses `32+12*(inputs+outputs)+32*nodes`; the revised compact wire has separately owned opcode-specific node lengths. Carried examples discriminate five descriptors, types/flags, u16 arguments, auxiliaries, u64 fields and nonzero high halves. Neither ABI is changed to match guessed parsers. |
| T03 | Valid all-zero EH words and plausible 168-cell factorizations did not establish the physical grammar. Whole-unit composition was supplied. | **Implemented and checked in C03; fresh acquisition pending.** Two carried blocks trace 1728 bits→24 EH72 words→192 decoded bytes→191-byte common block plus zero pad, with whole-unit repetition and no leading anchor. All 48 words run through carried generic recipes. |
| T04 | Table 17 has no caller among the retained 22 recipes; recipe 109 is only affine placement. First recipient inferred an equivalent ordering after help; fresh recipient received the exact multiplier/index formula. | **Implemented and checked in C04; fresh acquisition pending.** Carried geometry→table index→B/Q→slot→affine cell includes inverse, pad boundary, multiplier and wrap. Independent complete mapping and recovered relationship checks pass. Finite examples do not establish unique discovery. |
| T05 | VM examples progressed 20→24 after supplied byte-CONCAT, checked arithmetic, iteration and EMIT semantics. Residual submitted LSHIFT masks; examples are not full conformance. | **Carried discriminators implemented and checked, C02.** Actual examples cover overflow/failure/no data, immutable writes, byte CONCAT, EMIT offsets, iteration and checked arithmetic. CRC and UREM distinctions remain. Fresh recipient implementation and full conformance remain separate obligations. |
| T06 | Participant sliced common payload at 26 instead of 30, preserving total size while inserting/dropping 5116 bytes. Reviewer supplied the correction. | **Carried C06 repair checked; historical participant defect preserved.** Recovered definitions distinguish reserved26–29, payload30, CRC187–190 and decoded pad191. Fresh/stale checks and malformed widths/reserved/padding/fragment cases reject; copy agreement is not validity. |
| T07 | Saved tiny probes reproduced false exact acceptance of one section, bad local CRC and changed payload with stale CRC. Later assisted repair rejected them. | **Carried C05–C07 chain implemented; full damage evidence pending.** Checked inventory, physical group identity and completeness control availability; a lone checked section yields diagnostics, not content. Historical probes remain diagnostics; new accidental and deliberately reauthored boundary cases have distinct owners. |
| T08 | Participant chose largest valid tier as exact, exposed all when required was malformed, and accepted a checksummed zero ROOT entry. Repair followed explicit tier/type help. | **Carried C07 controls and semantic boundary checks implemented.** Bad all tier/root preserves required only; bad required exposes neither. Tests check output absence, types, references and budgets. Complete independent result/resource equality and corpus execution remain pending. |
| T09 | Negative profile2/7 was mistaken for status; valid EH/CRC bytes still could not establish the proper inventory. Surviving sections were only diagnostics. | **Carried C08 distinction implemented and checked.** Foreign profiles never establish profile8 or its streams. Their checked sections remain diagnostics. Profile, operation status, availability and artifact state are explicitly separate; fresh acquisition remains pending. |
| T10 | Section concatenation was mistaken for ContentStream; section IDs for record IDs; a namespace3 demonstration for a namespace2 game. Conventions/binding were supplied at different stages. | **Carried C09 namespace bridge checked against actual recovery.** Recovered sections100/200 bind the actual game/fixture records in their declared streams. The library preserves all64 games. Old record12 matrix and binding30/game31 are historical checks; fresh recipient use is still required. |
| T11 | Isolated terminal IDs29/182 failed a contiguous-from-one parser. The unfinished original was repaired in a reviewer copy. | **Carried C07 context distinction checked; provenance preserved.** Isolated terminal framing admits only its declared kind/ID/count; whole streams still start at1. Review-copy outputs remain attributed to that copy. Fresh independent use is pending. |
| T12 | Participant correctly computed e2e4 meaning but exported a success-wrapped 68 bytes and target20; the requested canonical67/target21 ABI had not been supplied. | **Packet ABI and carried C10 output distinction checked.** Recovered408-byte suffix contrasts67-byte Position with68-byte tagged result and absent0 versus target+1, and exposes Move16 fields. Prior correct meaning is credited. Later export instructions are supplied help, not initial discovery evidence. |
| T13 | First assisted damaged runs were isolated from clean input. Known damage recovered both streams yet missed sections239/266/267; unknown damage recovered required only. | **Historical success preserved; new full damage run pending.** New carrier and all10401 observation identities agree independently. Complete content can coexist with degraded artifact state. Per-input isolation, diagnostics and fresh outcomes remain required; old counts or clean bytes cannot supply them. |
| T14 | First submitted decoder skips raw repetition after a valid lane; fresh decoder is clean-only. No wrong supplied-case output was demonstrated from that shortcut. | **Carried C05 discriminator and regression implemented.** Both receivers collect valid lanes and raw-REP candidate, deduplicate full blocks and reject conflicts. Valid lane A plus distinct raw candidate B is explicitly checked. The historical shortcut remains a scope risk, not a demonstrated wrong supplied-case result. |
| T15 | Recipient shortcuts include ROOT budget as reference, high-half fields, partial typed checks and incomplete king safety/castling legality; fresh helper handles only the requested first pawn move. | **C02/C07/C10 repairs checked; narrow historical success preserved.** Recovered definitions and context tests cover widths, budgets, earlier references and canonical position/move distinctions. Owner validation of recovered games does not prove recipient full VM/content/chess conformance. |
| T16 | Fresh participant withdrew the claim that the 1408-bit physical tail is zero; its padding role came with later framing help. | **Carried C11 distinction and physical ownership checked.** The revision has832 deterministic physical pad cells, distinct from each decoded transport zero byte. Independent mapping assigns every cell exactly once. Fresh acquisition remains pending. |
| S01 | First result reproduced 44 generated files/four logs; fresh result reproduced 509 recovery files then 11 meaning files under different assistance and scopes. | **Preserved historical evidence.** Keep originals, reviewer copies, source regenerations and supplied answers distinct. New identities must be generated anew; three ordering hypotheses do not establish universal uniqueness. |
| S02 | Old learner's 134 commands, seven practice matches and 12/12 finals replay exactly on its 45-page prototype. No timing or sequence-of-viewing proof follows. | **Preserved formative score.** New final records contain no answer predicates/examples; owner scoring remains separate. New 65-page replay validates mechanics, not an additional human score. No biographies or timing reconstruction added. |
| S03 | Learner substantively learned occupancy, paired sides, alternating turn, piece patterns, EP removal, paired castle, promotions and opened-ray control. | **Preserved and extended in Lesson/UI.** Stable numeric rendering and distinct occupancy/control badges remain; pawn controls/friendly-occupied targets differ from legal destinations. Preserve actual oracle masks and same-board question distinctions. |
| S04 | A self-hashing manifest alone mismatched; all other rows/archive matched. Pixel equality, encoder bytes, score ties and regenerated external files have distinct scopes. | **Evidence limits preserved.** Exclude a manifest from its own preimage, verify real output sets/commands and deterministic artifact ordering. Do not call harmless tie/encoding differences content corruption or fabricate missing historical files/terminal logs. |
| S05 | Owner validated/re-encoded 64 games, 4915 plies and 10022 GameBytes. Learner prototype and old carrier have different content hashes. | **Games, independent compilation and development carrier→recovery→package bridge preserved.** Actual recovered streams match both compilers and the learner package. Complete damage, source, Gate8 and release checks remain pending. Old learner score or stream hash cannot qualify changed content. |

## Carried technical validation obligations

These obligations are implemented in the carried records described below and
checked by finite automated evidence from actual recovered bytes. Fresh
recipient use and complete production verification remain pending. Correct
English handoff or a host-only check cannot replace the carried distinction;
a second general VM or a recipe for every convention is not required.

| Repair | Relationship to preserve and validate in fresh recipient use |
|---|---|
| C01 | Oriented sector extent/scan and recipe header→descriptor→node boundaries. |
| C02 | Nonzero-width/type examples and bounded, checked VM composition/status/output semantics. |
| C03 | Physical cells→whole protected unit→EH/common/pad and whole-unit repetition. |
| C04 | Table17 geometry lookup→multiplier/slot→affine placement/inverse and pad boundary. |
| C05 | Physical group identity, all-lane/raw-repetition aggregation and conflict/inventory bootstrap. |
| C06 | Common/section fields, reserved/pad/length rules and domain-separated integrity checks. |
| C07 | Inventory completeness, distinct required/all roles, terminal context and typed/control validity. |
| C08 | Profile versus operation status; diagnostics/content availability versus artifact state. |
| C09 | Transport section versus typed content record/schema/binding and actual-game namespace. |
| C10 | Canonical Position versus result wrapper, nominal target convention and bounded move-record claims. Automated checks bind the physical-to-content bridge; fresh human acquisition remains unproved. |
| C11 | Deterministic nonzero physical pad ownership versus zero decoded transport pad. |

The exact historical boundary examples and minimum discriminators are retained
in `artifacts/work/participant-revision/technical-carried-repairs.md`; the ID
ledger above preserves every obligation even when those local work artifacts
are not distributed.

## Current validation and integration boundary

The current source freshly compiles 65 pages: 46 teaching, seven practice and
12 externally evaluated final pages. Required ROOT/entry are 588/12; all
ROOT/entry are 746/745. Required/all bytes match the independent Rust compiler:

| Stream | Bytes | SHA-256 |
|---|---:|---|
| Required | 42432 | `141168a051b44f978667f7c562070300d79368ace3fee47f5d19082de7642c17` |
| All | 55664 | `519cd24c0222ac2d21517a67bdb29ffbd8ce18c23d6ceef41b33d064fd8d9b8a` |

Fresh review ran 30 Python tests (Lesson, Slice and Packet), five Rust Slice
tests, generic layout tests and the complete 195-frame/194-command browser
interpreter replay. A direct all-stream JS run independently checked neutral
outcome3, passive/actual feedback equality, 74 payloads and Next→12. Every
selectable crop remains unchanged after whole-panel wrapping at 36 columns.
These checks are not a new participant trial or a recovered-carrier result.

The separately generated browser result was inspected at
`/private/tmp/gb-learner-revision-browser.0owwfx/browser-check.json`; its downloaded
session binds the required hash above. It exercises 65 pages, 199 commands,
66 commitments, one worked example, practice retry, RESET, a deliberately wrong
unscored final, a real export, narrow-screen readable-minimum controls and zoom
scroll preservation, with no external requests or JavaScript exceptions. This
is software/DOM evidence, not an additional human score.

Canonical installation and release remain pending. The development learner
package binds the recovered required stream, revised viewer and exact runner
mechanics. Final answers and owner scoring stay outside recipient material.

The current carrier includes every C01–C11 repair below and removes the unused
134-byte recipe106 from the active package. Historical/base106 is unchanged.
All29 retained recipes and14 tables have actual carried use; table17's observed
index227→11 and all48 whole-unit encoder calls are explicit evidence. Literal
grounding uses the carried prerequisite relationships, including iteration,
without running unknown instructions to invent their own grounding.

Fresh Python/Rust construction agrees on all12 static/carrier files. The
current carrier is520204 bytes including the count header, with SHA-256
`53ddc9714993549fb3bd4a0007ba945b42be5c2043902881bd3cf8bb83190e9d`.
Its2040/112 geometry,832 physical pad cells, protection factors, required/all
content and reserve remain unchanged; prefixes are25285 bytes each.

Both actual OBS_BITS receivers independently recover all136 checked sections,
all78 decoded bodies and both exact streams. Fresh knowledge evidence now binds
all78 bodies, not only the earlier illustrative game/fixture pair. The four
canonical recovery documents agree byte for byte: provenance28522 bytes
(`eb794ffc3fdc9be81eca2d90535f8c19267c9f7dd1e83d0419fa5639103402f8`),
knowledge89159 bytes
(`edba22f75db5f0d26cc41d9b76c6b1439cb23c928a2313773559038f7c551552`),
first-use126923 bytes
(`b2e5c4343cc0bcadfb3f3bc09771f803eb6ab29d0949f716951d1bc6c71de7fc`),
and the actual decoder result18084 bytes. The knowledge projection checks128
carried examples and96 presence/contradiction probes. Source-only receiver
bounds and all four correction/conflict/attempt boundary KATs also agree.
The fresh complete actual-receiver corpus, production integration and release
remain pending. No historical participant has been counted as a fresh pass.

The **512 KiB artifact ceiling remains a hard pet-project scope boundary**.
The initial uncompressed accounting did not fit the existing2048-side envelope.
The separate 2560-limit search first fits conditionally at 2440/112 and 744200
bytes, assumes route costs not yet proven sufficient, and exceeds that ceiling.
It is sensitivity arithmetic, **not admission or permission to enlarge scope**.
Compression/route reductions require owned bounded semantics, exact lossless
reconstruction and recomputed complete costs before any fit claim. No gate,
budget, release status or historical result is changed by this ledger.

## Historical development checkpoints

The following measurements retain their original scope and identities. The
current post-cleanup carrier and recovery evidence are identified above; an
earlier description of a then-final carrier does not supersede them.

The lossless revision now has a complete **development** carrier at side 2040,
shell width 112. Its physical matrix has 4,161,600 bits (520,200 bytes), within
512 KiB. All four complete route prefixes are 25,011 bytes each, including
5,173 bytes of actual numeric definitions, the generic content/control
miniature and its checked consequences, and the 18,407-byte recipe package.
The new explicit route version carries each table once inside that package;
only redundant standalone copies were removed. Required bodies still have
factor-five protection, all 64 games remain, future allowance stays 105,277
bytes, and uncompressed reserve stays 9,353 bytes.

The emitted physical matrix independently recovers exactly the required/all
stream hashes above, and the viewer preview was packaged from those recovered
bytes. Its carrier file SHA-256 is
`15340d8740294aa74b798941ba986de014470f6bfac757bae5bbcd287051fcf3`.
This is the first concrete source→matrix→recovery→viewer bridge for the revised
lesson. The development generator/report is separate from canonical Gate-8
and does not change a historical report or award a human pass.

The carried repairs are implemented as follows. Fresh acquisition and complete
release remain separate from these source and finite automated checks:

| Obligations | Concrete carried material and verified distinction |
|---|---|
| C01 | Facts 2/4/5: all transforms and sector corners/row boundaries, exact full-width envelope/package/table/descriptor framing, a five-descriptor recipe example. Actual matrix extraction reproduces all four complete prefixes; bytes after a declared prefix are ignored. |
| C02 | Fact 6 and recipes 210–214: all 25 operations, immutable WRITE, byte CONCAT, nonzero EMIT, checked quotient/shift/addition failure, output suppression and iteration progression. Python/Rust package bytes agree exactly. |
| C03 | Facts 7/8: two real common blocks and their 24-word EH encodings, 191-byte block plus zero transport byte. All 48 words were executed through carried generic encode/decode recipes. |
| C04/C11 | Fact 9: geometry/table17/slot/affine composition, nontrivial multiplier/inverse/wrap, protected/tail boundary. Complete mapping is independently checked in Python/Rust; the generated matrix assigns all 832 physical pad cells separately from transport padding. |
| C05/C06 | Facts 7/10: actual header/envelope layouts, fresh-check and stale-check contrasts, physical group allocation, locally valid lane A versus distinct raw-REP B conflict, and REP-only recovery from five individually uncorrectable lanes. |
| C07/C08 | Facts 11/12: inventory/dependency/ordinal/profile/availability distinctions, complete 575-byte generic miniature, 48 whole-stream mutations, closed role relationships, presentation-path counts, and eight exact runtime/budget sequences. Both-stream recovery remains distinct from complete artifact evidence. |
| C09 | Fact 12: explicit miniature/required/all contexts and actual section100 game binding588/opaque589 versus section200 fixture binding716/opaque717. Record numbers are qualified by their stream. |
| C10 | Revised state/address/target/rights and two-legal-choice lessons are in recovered required bytes. The408-byte route suffix explicitly contrasts canonical Position/result wrapper and Move16 fields. Recovered context checks pass; the supplied export handout remains disclosed assistance and fresh discovery remains pending. |

Fresh checks passed for all numeric definitions, content miniature, route,
compressed capacity, physical group aggregation and actual matrix recovery.
Independent Rust full-route/carrier generation now agrees byte for byte.
Complete damage/limits, archive-first transition, fresh native/Linux Gate-8
and release remain pending.
No participant needs another agreement, name/date form, or administrative quiz.


The completed C10 suffix now explicitly extracts a pure 67-byte Position from
the carried board/footer and contrasts a fixture's 68-byte tagged result with
its 67-byte Position. It also exposes Move16 fields and wire-admitted versus
legally applicable moves. Both positions are checked against public chess
replay; malformed wrapper lengths/tags, board orientation and target fields
reject. This adds 408 carried bytes without changing a lesson or final answer.
Python and Rust independently reproduce all four final 25,419-byte prefixes,
including 5,581 definition bytes. The final development carrier remains
2040/112 with the same physical capacity and content identities. Its SHA-256
is `170644adf4d9c9eb6d32939f23c88f3f905d13d7b0febc89d6095e7232fa2666`.
The earlier development carrier above remains a distinct pre-C10 measurement.
Fresh recipient acquisition and complete production gates are still required.

Independent Python/Rust construction now agrees on the final physical carrier,
all136 section payloads, both recovered streams and all seven static projection
documents. The physical matrix remains520200 bytes; required protection,
future105277-byte allowance and uncompressed9353-byte reserve are retained.
The nine-file offline learner package built from recovered required bytes is
191807 bytes with SHA-256
`5f8d638966f90a9eb0ae95f87171009414aed2029457afaab3657818f2a41588`.
Final answers and scoring remain outside recipient material.

Observation-only semantic validation now consumes the numeric definitions and
checks their relationships to recovered content, including section-versus-record
namespaces and the67-byte Position distinction. These software checks do not
replace fresh recipient discovery. The independently generated damage inputs
agree for all10380 accidental observations plus21 separately reauthored boundary
probes. A38-observation semantic preflight agrees with the source-side oracle,
covering missing data, conflicting copies, foreign profiles, malformed body
codecs and invalid required/all tier controls. Full independent result/resource
equality, complete corpus results, physical proof, archive-first replacement and
fresh native/Linux release remain pending. No historical human score is reused.

The complete38-row semantic projection also agrees byte for byte between the
independently built source oracles. All96 DEFINE removal/contradiction probes
pass with file access and source-route construction disabled during admission.
Removal checks structural presence; the contradiction checks and recovered
context checks establish separate finite uses of the carried relationships.
These are automated checks, not another participant score. All31 original
findings remain in this ledger, including successful learner observations and
the limits imposed by supplied technical assistance.

The finite carried-use artifact was also generated from a fresh observation-only
decode of the actual C10 carrier, extracting its four observed prefixes, both
recovered streams and checked game/fixture bodies. Its81029-byte canonical
evidence has SHA-256
`ccb4376fe2360551436e970854ee4f201b90845563322ef11e3cec2f292f1ecf`.
The separate recovery provenance binds those exact inputs. This closes the
development recovered-byte link for those finite checks; it does not establish
complete Gate5, a damage pass or fresh human acquisition.

## Current source-integration checkpoint

The later bounded re-audit covered all 31 rows against the actual revised kit
at `artifacts/work/participant-revision/participant-bundle-rust-v2/` and its
tracked sources. It found no missing material teaching repair within that
review. The 42,432-byte required stream, ROOT 588, local budget 16, global
budget 4160 and the twelve-final assessment were revalidated against authored
intent and public chess. The original assistance and acquisition limits above
remain unchanged. This is source/kit review, not a fresh recipient score.

The final independent Python/Rust comparison matched all 36 raw technical and
learner bundle files. Its development comparison digest is
`e6bc8f732f30b3120349072fee4d525b3d23b0413c608e41b133cea642f1d60a`.
The six actual foreign-profile D7 cases also matched in all twelve canonical
result/resource documents, comparison digest
`35d7fd9e35b8f5ee4b7dbb72336215a11b821fead212760cda32bd6413c54de4`.
These checkpoint identities describe those comparisons; they are not receipt,
complete-corpus or release identities.

The review found and fixed one final mechanics boundary in
`tools/m2/learner_session.py`: the command limit now derives as twice the
validated ContentRoot global budget, rather than from owner metadata or the
former fixed 8192 ceiling. The executable 8320-command/4160-commit witness
accepts as an exhausted, incomplete attempt; 8321 commands reject. The 575-byte
historical miniature with ROOT budget 8 rejects 17 commands even when owner
metadata claims 4160. Fifteen session, assessment and runner-v1 tests passed
in 24.820 seconds, following 28 semantic/assessment/mechanics checks in 2.400
seconds. Recipient bytes, final questions and score thresholds did not change.

The clean-Linux adapter and root component/verification dispatch have twenty
passing focused and inherited orchestration tests (14.662 seconds). These
exercise the actual shell boundaries and transient parser with explicitly
simulated external jobs. No Docker, complete component suite, full producer
or release success is claimed by those tests. The archive-first production
transition, complete frozen current evidence, fresh native/Linux Gate8 and
composed release remain separate required work. The revised source documents
preserve `In progress`; no historical human result is relabeled.
