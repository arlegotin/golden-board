# M2 complete recovery teaching implementation plan

Owner authorization: proceed with the trajectory review's structural repairs,
using judgment without another approval ceremony. Work in the existing shared
development checkout and preserve every historical submission/frozen packet.

Goal: replace the opaque host-side connection between primitive demonstrations
with a carried executable recovery procedure, and disclose the saved-method
boundary before clean readiness. Success in source is not human acquisition.

## Architecture and alternatives

The current roster and masked Boolean decision depend on an unwritten host
adapter. Adding a further table/transcript alone repeats trials 11–13. A new
orchestration language would introduce another interpreter to discover. Instead
use the existing 25-operation VM for a complete arbitrary-input group procedure
and executable physical roster derivation. Ordinary examples must invoke that
same procedure; raw observations and admitted inventory fields supply its
inputs. No owner classifier flags are accepted as inputs.

The VM propagates failures, so internal bounded error-correction results must
be represented as data while machine/resource failures remain failures. Every
lane and raw repetition contributes to the complete candidate union. Expected
identity comes from inventory/physical position before decoded headers.

Measured current package node compression offers approximately 7.5 KiB using
canonical bounded unsigned base-128 fields. Implement the exact independent
codec in `spec/recipe-wire-v2.md`; retain all logical resources and historical
formats. Measure full program and examples before changing active packaging.
Try to preserve all 64 games within the existing 512 KiB and geometry bounds.
No fit is claimed before actual generation.

## Tasks and verification

- [x] Align opening/readiness/adapter/held-out instructions in one template
  source, with explicit prospective ownership and no decoding hints. Controlled
  substitution regression and participant packager/mechanics tests: 18 passed.
- [x] Implement independent canonical wire-2 codecs. First pin old-entrypoint
  rejection, exact logical round-trip, scalar boundary/overlong rejection,
  trailing/cross-recipe rejection and unchanged VM results/resources. Compare
  Python/Rust bytes on the same logical source, not each other's encoded input.
- [x] Measure a complete generic-opcode group prototype. Pin all-lanes-corrupt
  but REP-recoverable, intact-lane versus conflicting REP, unknowns, absent
  lanes, foreign identity and no cross-group relocation against existing
  production semantics. No production decoder is called inside VM execution.
- [x] Own the final complete ABI, physical roster, ordinary invocation examples
  and exact byte/resource bounds before integrating active route generation.
  Independent source builders and observed admission must execute the same
  carried procedure with fully derived input bindings.
- [x] Run affected Python/Rust source/receiver/resource regressions, fast checks,
  actual carrier fit/clean recovery and bounded preflight on planned observations.
  Diagnose actual failures; do not launch exhaustive qualification for readiness.
- [x] Independently review the complete change, including task neutrality,
  erased-header membership, all-candidate conflict handling, total-adapter failure
  scope, malformed wire, operation/scratch accounting and source independence.
- [x] Record exact evidence and prepare the smallest useful formative transfer
  material before any fresh qualifying handoff. Do not present an owner replay
  or a primitive example pass as demonstrated recipient acquisition.

The native/Linux/full damage/release barrier remains after stable required
technical and learner successes. There is no automatic new fresh recruitment,
retroactive pass or content reduction merely because implementation compiles.

## Integration evidence recorded so far

The final package is17,719 bytes (41 programs,19 tables); its expanded logical
package is102,313 bytes. SHA256:
`ac7416799ea67526acfe10c5e1698f78973e1aaa790c02ec3c97d0e189717da4`.
Four independently constructed Python/Rust prefixes agree at25,360 bytes each.
Their44 observed route calls charge152,936,421 declared VM steps and98,398 bytes
of VM scratch per route. Actual independent carrier construction chooses
S2040,W112,Q1908:520,200 carrier bytes plus the four-byte observation header.
All64 games,105,277 future-authoring bytes and9,353 reserve bytes remain.

Production square recovery now invokes127 for provisional first inventory,
120 for subsequent/full expected groups and123 for every admitted roster group.
A unique wrong-owner packet keeps its local diagnostic while failing group
acceptance. Complete candidate union precedes identity; a matching candidate
cannot erase a conflicting foreign candidate. Independent oracle regressions
exposed and repaired this same ordering defect and diagnostic loss.
OBS_UNITS and historical encodings retain their existing schedules.

Focused codec, generic/native equivalence, typed-domain, closure-mutation,
cold-filesystem, route, first-use, operational, resource, bounds, capacity and
static checks have passed in bounded runs. The final new Python fast modules
ran 40 tests with four explicit generic opt-in skips. The generic source-carrier
transfer and complete construction cases also passed their earlier opt-in runs.

Fast coverage is composed from the original aggregate plus repaired-module and
unreached-target runs. The original aggregate failed; do not relabel its exit.
Its failures exposed old wire/version mutations, stale geometry assertions and
an ablation boundary tied to the previous examples. Repairs preserve the
rejection and exact-resource assertions. D7 now locates the actual first node
through compact descriptors and changes only the intended opcode or type bits.
All remaining Rust fast and slice targets subsequently passed. No unchanged
expensive suite was repeated solely to obtain one green aggregate transcript.

The independent Python and Rust finite preflight initially agreed on all 22
files, including 132 carried examples, 21 B0 cases and four KATs. Final exports
also agree byte for byte after the profile/damage owner migrations and diagnostic
repair (Python 470.166 seconds; Rust 14.71 seconds). This preflight generates no exhaustive D0–D7 corpus.

The new conflict observation also exposed a production Rust diagnostic defect:
a valid foreign lane was relabelled corrupt after catalog-key comparison.
Group rejection was correct, but the local evidence and scratch accounting were
not. The receiver now preserves that lane's local state/hash while the complete
group still rejects foreign identity or conflict. A reproduced failing test
passes for both a unique foreign packet and foreign-original versus REP conflict.

The reviewed handoff uses the participant from round 13, first the full revised
clean carrier and then two full unseen matrices. It discloses the saved-method
boundary before readiness, permits prior work and later diagnostic revision,
and adds no questionnaire. Case a requires its actual optional transport object;
content hashes alone cannot stand in for that recovery. Case b must withhold
the conflicting required section. Final packet readiness requires source-oracle
semantic agreement and exact Python/Rust result/resource bytes for all three
observations, with the source and executable frozen. The packet's owner evidence
records that admission; this plan does not substitute for it.

The local handoff is now frozen at
[`artifacts/quiz/14-complete-recovery-formative`](../../../artifacts/quiz/14-complete-recovery-formative/README.md).
Clean, a and b all passed the separate source-semantic comparison and exact
Python/Rust result/resource-byte comparison with zero wrong accepts. Clean and
a are exact; b correctly withholds the conflicting section. All 1,104 inventoried
files were checked after copying, including the six recipient files. Final
repository checks passed (58 tests plus shell, formatting and diff checks).
The packet retains its complete source snapshot and both 22-file preflight
trees. This completion note was added after freezing that source; it changes
no result-bearing code, owner, observation or participant task. M2 remains
In progress. At that freeze, the formative result was still pending.

## Completed formative result and next handoff

The [trial-14 answer review](../../../artifacts/quiz/14-complete-recovery-formative/review/ANALYSIS.md)
independently replays the unchanged participant method on clean/a/b. Clean and
a recover all 136 exact envelopes and both lesson streams. The erased-header
two-copy group is recovered through original raw evidence. The five-copy
foreign-lane versus repetition conflict in b is refused; 135 other envelopes
remain exact and neither dependent stream is exported. All deterministic replay
files match; the 46,346 submitted files remain unchanged. The physical-ownership
and complete-candidate connection is now part of live recovery. Prior experience
was permitted, so this remains successful formative transfer rather than a
fresh qualifying result or proof that the carrier alone caused the improvement.

No new teaching change or question to this participant is indicated. The normal
fresh technical handoff is prepared at
[`15-fresh-technical-complete-recovery`](../../../artifacts/quiz/15-fresh-technical-complete-recovery/README.md)
on the same carrier. The real production builder supplies its unchanged four
stages and all 25 recipient/owner files. Fresh Python finite preflight equals
both retained 22-file own-language trees; the inherited Rust preflight is labeled
as such. Fresh independent Rust/Python decoding and separate source-semantic
comparison pass on clean and all four actual standard observations. The owned
recovery promises separately pass for all four, including explicit D7/11 refusal.

The packet retains the 606-file frozen source, production manifest, exact
expected outputs, executable, command logs and source reconciliation. The only
differences from trial 14's checked source before packet generation were two
historical prose files. Focused coverage is inherited with its original failed
aggregate and successful corrective/unreached runs intact. These result notes
are added after the packet freeze and recorded separately; they change no
result-bearing code, owner, handout, expected answer or acceptance condition.
M2 remains In progress. Fresh technical and required learner success, complete
automated verification and exact reconciliation remain outstanding.

## Trial 15 result: unfinished work before the next design decision

The [trial-15 review](../../../artifacts/quiz/15-fresh-technical-complete-recovery/review/ANALYSIS.md)
reproduces the submitted partial method. Clean has all 136 exact envelopes and
both streams; a has all 121 expected envelopes and its required stream. B has
114 exact envelopes against 124 recoverable, and loses the required stream.
C/d both parse storage but report unsupported recovery. The same code freshly
passes all 132 embedded examples and produces exact requested record, move and
position bytes. These successes do not supply the unimplemented live physical
ownership/raw-candidate connection or unit-stream recovery.

The clean account expressly declines readiness, and the adapter declares only
adapter readiness alongside that unchanged partial method. No later complete
declaration appears in the saved record. Do not infer a declared-ready failure
or external handoff chronology. The next action is the
[same-participant continuation](../../../artifacts/quiz/15-fresh-technical-complete-recovery/continue/README.md):
finish the admitted integration work using the artifact's own evidence and
save the new method/output separately. Already exposed cases are diagnostic;
the original result and retry rules remain unchanged.

Keep scope to the existing recovery contract and requested field/reference
explanation. Full generic lesson-engine implementation, opaque-body meanings
and player histories are not new requirements. No result-bearing source or
carrier change is made in response to this incomplete checkpoint. Fresh
recruitment and exhaustive checks remain deferred while this investigation
continues. M2 remains In progress.

## Trial 15 diagnostic completion and content correction

The [continuation audit](../../../artifacts/quiz/15-fresh-technical-complete-recovery/review-continue/ANALYSIS.md)
reproduces the live transport connection, correct unit-stream recovery/refusal,
exact source-bound outputs and the participant's optional parity/CRC extensions.
It preserves the original unfinished submission and known-input diagnostic
status. No new recovery redesign or task expansion is justified by these results.

It also isolates a content rival that all previous examples permit: SINGLE
replace-last versus retain-first at capacity, plus duplicate result versus
exhausted phase. The smallest owner is `spec/content-teaching-v2.md`. Append two
runtime-derived rows; retain all old rows and content-v0 behavior. This adds64
bytes per sector: supplement1120, framed miniature1703, fact12 value2485,
complete prefix25424. Bind the precise input witnesses as well as independently
recomputing their consequences. The participant's requested task stays unchanged.

- [x] Reproduce the saved continuation in isolated input/source directories;
  compare every published envelope, stream and query output with frozen evidence.
- [x] Demonstrate the missing carried witnesses with failing Python/Rust tests;
  add the two rows and reconcile both independent builders/admissions and sizes.
- [x] Complete focused checks, actual source-built carrier/clean recovery and
  planned handout comparisons; freeze a revised provisional technical packet.
- [ ] Run the ordinary fresh technical trial and required learner trial; preserve
  readiness, assistance and retry limits. Then run full qualification.

Prediction before the next exposure: the added contrasting rows distinguish
capacity, reset and last-action result from phase, while the complete live
recovery connection continues to transfer. A repeated structural problem calls
for representation/scope review, not another unexplained unchanged recruit.
No further assignment is needed from participant15. Earlier successful and
unsuccessful findings remain in the ledger. M2 remains In progress.

The checked next handoff is
[`16-fresh-technical-content-control`](../../../artifacts/quiz/16-fresh-technical-content-control/README.md).
Fresh independent Python/Rust preflights agree on all22files. Clean and all four
actual standard handouts pass full result/resource equality, separate source
semantics and owned recovery promises. `scripts/check fast` passes:520 Python
tests (7 explicit skips),179 Rust passes (1 opt-in ignore). Original direct
regression failures and corrected runs are retained with the new packet.
Carrier520204bytes, SHA256
`9a51ec9f89df35b811b5660f754d9f4e4516a5a0d61b6610e8784cf05f73d2e0`;
all64games and both lesson streams are unchanged. Every old teaching row is
preserved. This records provisional readiness, not a new human or full gate pass.
