# M2 executable physical construction repair

Owner authorization: proceed after trial12 reconciliation. Implement in the
existing M2 development checkout; preserve all participant inputs and frozen
trial12 bytes. No canonical Gate8/report promotion or exhaustive run here.

The demonstrated failure was discovery and joint checking: all ordinary
examples could pass while construction descriptors were misparsed as BHB and
the contradictory erasure witness was executed separately. The repaired carrier
must expose construction on the ordinary executable-example path, distinguish
four fields by typed ABI and orthogonal flip/erase examples, and connect case7
through raw symbols to its exact erasure input. Preserve conflict, REP-only,
physical ownership, complete-block equality and unknown-storage lessons.

## Owned design

See recipe-teaching-v2.md, route-definitions-v2.md, route-v2.md and
resource-accounting-v2.md. Active duplicate CRC111 becomes a generic BITS72
constructor; CRC examples use identical107. Table22 is BITS144 zero72/ones72.
No new VM opcode. Inputs A,B,source BOOL,unknown BOOL,first UINT8,count UINT8;
outputs STATUS16,observed BITS72,mask BITS72. All64 games remain exact.
Fact10 becomes443 bytes;48 route records and33 framed examples;62 actual VM
calls. Neutral bounds cover max(route_records+3,62), still259 with256 ceiling.
Expected prefix25791 bytes versus25809 prior; verify actual generation and
geometry before calling the next packet ready.

Byte offsets in fact10: constructor101..104; template header104..106;
13 descriptors106..158; decision158..160; case header160..162; cases162..354
(trace174+24*i); coordinate header354..356 and rows356..368; witness368..373
(case7,source0,word0,recipe30); input373..386; repetition header386..390;
four rows390..422; five fragment rows after count422, ending443.
Template source0=A/1=B; template2=(0,0,59,5), template9=(0,1,59,5).
Framed1002/1003 invoke111 on templates9/2 and both fact8 first words;
1004 invokes30 on case7 input with fact7 A first8 as result. All thirteen
constructor calls are charged before eight decision calls. Both language
receivers and the independent Rust source oracle preserve identical early
rejection/charge order.

## Execution

- [x] Add failing constructor and connected-example regressions; implement
  Python source generation/admission and independent Rust generation/admission.
- [x] Update exact owner pins, resource accounting, static capacity and focused
  semantic, recipe, route, receiver and bound regressions. Preserve historical
  package identities and all existing physical contrasts.
- [ ] Build actual candidate/handouts from the final source, compare independent
  Python/Rust preflight outputs, run39-case preflight and handout readiness.
- [ ] Review independently, freeze source and provisional trial13 packet using
  existing participant tooling, record exact checks and limits.
- [ ] Give the owner a simple fresh technical handoff; no consent/forms/names/
  dates/timer. Keep exhaustive qualification after stable required human trials.
