# M2 physical recovery context repair

**Goal:** Make the carried recovery examples explain physical ownership,
identity comparison, raw repetition and bit coordinates before another trial.

**Spec:** `spec/route-definitions-v2.md` fact10 and `spec/route-v2.md`.
The owner approved the continuation review's repair scope and instructed us to
proceed. Keep working in the existing `m2` checkout, preserving the review and
all original participant files. No merge, publication or long verification run.

**Architecture:** Reorganize fact10 within its existing430 bytes. Its finite
roster uses u16 fields; explicit keys, physical scenarios and numeric witnesses
replace duplicated statistical summaries. Python and Rust construct and validate
independently. The existing package, content,32 framed examples,47 route records,
512KiB ceiling and shell headroom remain. Eight110 calls, one30 and three113
calls bring the complete route to47 VM calls, within the existing50-call bound.

## Tasks and verification

- [x] Add regressions that fail on the old representation: explicit physical
  ownership changes the identity outcome for identical A; corrected A does not
  set the verified-lane flag; preserved unknowns recover A while stored-zero
  guessing fails; exact coordinate/input correspondence and repetition tie,
  disagreement and unknown columns are executed and charged.
- [x] Implement Python generation, independent observed semantics, primary
  binding and charged witness execution. Keep the DEFINE length430 and last
  byte outside VM inputs/outputs for knowledge-use rejection accounting.
- [x] Independently implement the same owner in Rust generation, semantics,
  observed receiver and damage oracle. Check mutated contexts/keys, template
  erasures, coordinates, witness inputs and flags reject with matching costs.
- [ ] Run affected component tests and `scripts/check fast`; fix actual failures
  without widening bounds. Review both implementations and evidence limits.
- [ ] From the final source, build and compare actual Python/Rust preflight
  trees and run the39-case Python/Rust/source-oracle preflight selection. Check
  every proposed participant observation and both unchanged lesson streams.
- [ ] Freeze source, production recipient/owner files, logs and expected results
  under `artifacts/quiz/12-physical-recovery-technical-v2`. Provide the existing
  simple staged handoff and empty answer folders. M2 remains In progress.

## Review focus

- Header-based reassignment must not substitute for physical ownership.
- Identity comparison covers all eight fields, not just section ID or CRC.
- Unknown stored zeroes and absent/out-of-factor lanes remain distinct from
  observed zeroes. Failed lanes still contribute their original observations.
- The ninth flag's derivation and verified/recovered distinction must be carried,
  not merely asserted by an owner-only lookup.
- Every actual VM call and early rejection must agree across receiver/oracle;
  neither metadata compaction nor focused replay weakens final gates.

## Progress

The review identified the missing relationship before implementation. The owner
layout is exactly430 bytes; no geometry expansion or new recipe is planned.
Detailed transient command logs will be retained with the frozen trial so logging
results does not mutate its source projection after freezing.

The new Python regressions first failed on the old physical roster and missing
coordinate binding, then the32-test route/policy suite passed in75.814s. Rust's
independent route, observed-receiver and semantic suites also passed. The source
review found and resolved one owning ambiguity before implementation: all-absent
REP is state0, not a failed construction. A corrected erasure-position mutation
can still return A; independent context validation must reject that wrong input.

The source design was reviewed independently; its exact offsets and47-call
schedule reconcile. Full implementation review, fast checks and frozen packet
readiness results live in the trial owner logs; this plan is preserved as part
of the source used to create that packet. All32 framed examples and every final
acceptance requirement remain. The owner permits reducing games if clearer
teaching needs the space, but this430-byte repair retains all64 games.
