# Two source-generated formative recovery observations

This owner defines two bounded development observations for an already exposed
technical collaborator. They test reuse of a saved general procedure on the
current complete carrier. They are not fresh-participant evidence, D0–D7 damage
qualification, or permission to change any historical packet or passing gate.
Previous exposure prevents attributing acquisition to this carrier alone.

The input is a freshly source-built `DevelopmentCarrier` with its capacity plan.
No participant submission, historical observation, or production decoder output
is a generation input. Select sections by ascending source section ID and use
fragment index2, requiring at least three fragments. Case a selects the first
noninventory section with physical factor2. Case b selects a different factor5
noninventory section, preferring required section16, then17, when eligible;
otherwise use the first required eligible section. Fail closed if selection is
unavailable. The foreign packet for b is fragment2 of a's section, a distinct
real physical owner with a fully valid profile8 common block.

Case a marks encoded bits0..4 unknown in its first member and10..14 unknown in
its second. Both individual first EH words are uncorrectable under the bounded
procedure; raw repetition reconstructs the exact source common block. Case b
replaces member0 with the foreign encoded packet and flips bits32+2i and33+2i
in members1..4 for i=0..3. Those four individual first EH words are uncorrectable;
raw repetition reconstructs the original source packet. The distinct foreign
original and original-owner repetition packet must conflict before expected
identity comparison. No packet may migrate to another physical group.

Both outputs are full OBS_MATRIX observations: big-endian side:u16 followed by
side² row-major cell bytes0/1/2, where2 means unknown. All changes use the actual
two-stage source placement. The shell and every other cell remain unchanged.
Before emitting, the builder checks source common grammar, binds each affected
original lane to the actual clean carrier, and independently reconstructs local
EH and raw-repetition candidates using bounded codec primitives. It requires the
specified unique-recovery/conflict properties; no production recovery program
or receiver supplies expected outputs. This is a selected-group construction
check, not a replacement for complete source-carrier validation or full
section/content admission.

The builder returns immutable observations plus owner-only source hashes,
physical selections, exact changed cells, common blocks and expected group
states. The absent foreign-common hash for a uses the existing64-zero SHA
sentinel so owner metadata remains canonical manifest-v0 data. Recipient files
contain observation bytes only. Names a/b convey no
damage mechanism or answer. Owner metadata must remain outside the recipient
tree. A decoder run is verification, never generation authority.

Use the full clean carrier first, with the existing neutral task disclosure:
readiness saves a general method; subsequent observations may include damage or
missing data; only storage adaptation may be added afterward. Release these two
observations after saving the method. Request existing code, reproducible
outputs and concise notes, without revealing physical IDs, factors, algorithms,
case mechanisms or expected answers. Do not add forms, timers or another required
explanation task. Any assistance and later substantive method revision remain
visible formative work. Exact recovery where supported and withheld output on
conflict distinguish transfer from replaying examples, but do not award an
unassisted acquisition claim or relax a final gate.
