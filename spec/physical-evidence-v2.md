# Revised physical independence evidence

This owner preserves the first eight predicates and violation units of
damage-policy-v1's independence_manifest, with the explicit profile8 mapping,
inventory2, slice and ownership-v2 meanings. It does not emit a Gate7 proof
without the ninth predicate binding complete freshly passing damage evidence.
No historical witness count or historical proof is a current input.

Inputs are exact canonical candidate-manifest/v2, capacity-ledger/v2,
ownership-ledger/v2 and semantic-envelope/v2 bytes. Validate closed static-projection-v2 shapes,
candidate self identity, raw hashes and lengths before enumeration. No carrier
builder, production receiver, expected decoder output or saved proof is an
input. Geometry remains S<=2048, W<=128; every loop follows the existing
bounded section/unit/dependency domains. Reject malformed input, including
booleans in integer fields, rather than coercing it. Well-formed contradictory
evidence produces counted violations, never omitted witnesses.

Use S/W/Q/P and the admitted two-stage mapping from bootstrap-v2. Recompute
the smallest admissible slot multiplier from the bounded row-carry predicate.
For every protected lane bit and logical-tail pad cell, independently enumerate
its physical target, target multiplicity, inverse identity and round trip.
Reconstruct the complete 9-byte ownership table in row-major order; pad offsets
are logical-tail offsets as owned by ownership-v2. Hash every lane's physical
indices, compare its ownership row, and count exact shell/class totals.
Each candidate route-prefix file length binds exactly to its shell's prefix
cell count divided by8. Prefixes are distinct from full shell images, whose
hashes include headroom and fixed pad. Require every instruction/example/recipe
span to lie wholly inside the prefix, every headroom span inside the declared
headroom, and every fixed-pad span inside the tail; adjacent equal-class spans
must have been coalesced. Count each sector with invalid span ownership as one
additional shell-partition violation, and each mismatched five-class ledger
total as one owner-factor-ledger violation.

The required spine is exactly1,2,3,16,17,18 with factor5. Other real bodies have
factor1. Capacity owner names bind factor2 for replicated-core0-2 and factor1
for nonreplicated-core3-4; reserve uses factor2 and load factor1. Capacity bucket
classes and exact real/tier assignments come from the bound semantic envelope,
not from guessing a class from the factor under test. Exact section types,
owner identities, factor groups, contiguous IDs, slots and dependencies retain
their static owner. Lane separation checks each corresponding encoded bit of
each unordered factor2/5 lane pair in final physical coordinates; separation
must be at least max(32,floor(I/8)) in one coordinate.

Recompute all256 owned D2 placements independently using the unchanged sampler,
including rank-unfiltering the nine anchors. D2's square side is exactly
max(32,floor(I/32)); the larger lane-separation window does not define D2.
For each square, invert its cells
and enumerate erased symbols per physical lane and raw-repetition candidate.
A required fragment survives if any lane has at most3 erasures in every72-bit
word, or the raw repetition leaves at most3 unresolved symbols in every word.
All D4 omissions and all four D6 sector+unit removals must leave another lane
in each required group. D6's erased shell sector cannot affect protected
interior cells. Dependency edges must target declared sections and match the
semantic tier assignments: tier2 names the required bodies, while tier3 names
all bodies, including all-only bodies. The remaining sections have no
dependencies. Dependency witnesses enumerate the expected semantic body targets
for these two tiers, not the potentially damaged declared edge list. Each
missing or wrong expected target counts one edge violation; deleting an edge
cannot shrink the witness count. Extra/unowned edges additionally fail the
global semantic reconciliation without creating new expected witnesses.
Four single-sector removals each leave exactly
three independently complete route prefixes. Reconcile group/unit/pad/cell
and byte ledgers before any closure claim.
Here prefix completeness is a premise supplied by fresh static/Gate5 source
validation. The four-manifest physical projection proves survival of those
bound extents, not raw route semantics. A complete Gate7 proof must bind that
fresh validation's exact identity as well as complete damage evidence; these
development rows alone cannot establish the premise or award Gate7.
Predicate7 measures physical survival from the actual unique correctly placed
lane members. A wrong retained hash or owner label alone is a violation of its
own metadata predicate, not an additional physical-survival violation. Missing,
nonunique or misassigned members are unavailable lanes for survival arithmetic.

Predicate order and exact witness formulas:

1. two-stage-map-bijection: P.
2. matrix-owner-total-partition: S².
3. shell-sector-total-partition: S²−P.
4. physical-group-total-partition:1728Q.
5. owner-factor-ledger-reconciliation:S².
6. final-cell-lane-separation:1728×(G2+10G5).
7. d2-d4-d6-required-closure-survival:6×(256+Q+4Q).
8. section-dependency-inventory-closure:G1+G2+G5+dependency_edges+4+1.

Each row has predicate_id, witness_count, minimum_surviving_count=1,
violation_count and result=pass iff violation_count=0 and the exact positive
witness count is present. Count full bounded enumerations; no early success.
Logical group domains enumerate every declared section fragment with its
semantic-owner factor, including empty or malformed actual groups. Bound their
aggregate factor-expanded positions to2389 before enumeration. A changed unit
identity or factor cannot remove declared lane-pair witnesses; a missing or
nonunique member, or one with the wrong canonical physical ID for its replica
position, makes each affected corresponding-bit pair a violation.
The development output is canonical golden-board.m2-physical-evidence/v2 with
profile_id, scope=physical-predicates-only, input_sha256 (candidate_manifest,
capacity_ledger,ownership_ledger,semantic_envelope) and predicate_rows. It has no overall Gate7
result, damage identity, promotion receipt or ninth-predicate placeholder.
The complete proof owner must bind the ninth predicate and fresh independent
Python/Rust agreement before production admission.
