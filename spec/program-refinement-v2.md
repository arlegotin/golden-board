# Profile-8 program refinements

This development owner permits bounded execution refinements. It supplies
neither knowledge-acquisition evidence nor production promotion. The receiver
first parses and completely validates the actual carried compact package.

The mapping extraction fast path requires recipe109 to have exactly the
source-owned logical interface and nodes of bootstrap-v2's profile8 affine
program. Compare every input/output type and width, node opcode, result type
and width, ordered arguments, both auxiliary fields and full u64 immediate.
This program has no table or program dependency. The source construction is
`m2_recipe._r3_fact(109,8)`, implementing the neutral mapping algorithm without
lesson, route, candidate, geometry or expected-result data. Three executed
samples are additional checks, not proof for arbitrary replacement programs.
A different logical program is not admitted by this refinement; route2
acquisition rejects it rather than treating its samples as universal equality.

Body202 may use body-codec-v1's native decoder only when its complete transitive
logical closure agrees: recipes201 and202 and referenced tables3,4,5. Compare
all fields above, then each table's ID/type/width/count/payload. Independently
construct this neutral closure from body-recipe-v1; never regenerate a route
or compiled content. Unrelated programs and tables do not affect this test.
A different observed closure falls back to execution of its actual generic VM.
No package SHA alone substitutes for these structural comparisons.

The refinement relation maps the full VM state to input prefix length, declared
decoded length, input cursor, output cursor, flag position, remaining copy run,
and the initialized output prefix. Each recipe201 iteration emits at most one
byte. A literal consumes its flag/literal; a copy validates its two-byte token,
distance1..4096 and length3..18 before reading only prior output, including
permitted overlap. Source and target cursor bounds, exact EOF and unused low
flag bits are checked before success. Recipe202 initializes the same fields,
performs exactly16384 iterations, then returns the same decoded length and
zero-padded16384-byte output or nonzero status with no output. Native rejection
maps to status3 for the admitted byte/length interface. Logical declared202
cost is charged even on failure or cache reuse; native speed changes no charge.

A finite refinement check runs the generic202 and native implementation on
valid raw-token and overlapping-copy examples, unused input-array padding,
malformed token/distance/flags/EOF/header bounds, and the maximum decoded body.
It compares status, length, complete zero-padded output and absence on failure.
Existing independent Python/Rust full-body and reachable201 boundary tests
remain necessary. These checks support the stated program refinement; they do
not establish a participant's independent inference or replace full damage.

Native transport likewise requires exact logical transitive closures for the
carried EH72 decoder30 and repetition113, including every referenced program
and table. The neutral baseline is the independently verified bootstrap-v1
R3 transport kernel; those programs are unchanged by profile8. Program109's
profile constant is outside their closure. Altered transport closures reject
at route admission; finite worked examples alone cannot authorize native
execution of an arbitrary replacement. Existing full EH/repetition and
cross-language KATs establish this unchanged kernel's refinement.

Regardless of optimization, body202 must retain its owned BYTES16384/UINT16
input interface and STATUS16/UINT16/BYTES16384 output interface. Full generic
validation derives its resource declarations. This bounds cached results and
ensures the runtime's input/length and atomic output conventions are exact.
